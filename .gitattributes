# 行尾策略：仓库与所有平台的工作区一律使用 LF。
# 背景：Windows core.autocrlf=true 曾造成部分文件工作区 CRLF 与索引 LF
# 不一致，git status 出现无法提交的假差异（2026-09-13 记录，见
# DEVELOPMENT_PLAN.md 实施日志）。本文件固化策略后不再依赖本地 autocrlf 配置。
* text=auto eol=lf
